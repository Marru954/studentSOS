services:
  postgres:
    image: postgres:16-alpine
    container_name: uniportal-db
    environment:
      POSTGRES_USER: uniportal
      POSTGRES_PASSWORD: uniportal
      POSTGRES_DB: uniportal
    ports:
      - "5432:5432"
    volumes:
      - uniportal_pg:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U uniportal"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  uniportal_pg:
